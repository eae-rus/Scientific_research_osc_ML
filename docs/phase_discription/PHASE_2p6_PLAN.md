# План проведения экспериментов (Фаза 2.6: Архитектурная эволюция и Финальная калибровка)

-> **Статус:** Завершена
> **Цель:** Переход от «перебора параметров» к «архитектурному творчеству», проверка гипотез о раздельной обработке каналов и гибридных моделях.

Этот план включает в себя как проверку новых архитектурных гипотез, так и перезапуск лучших конфигураций на исправленном коде.

**Мой комментарий**
Не забыть вести лог PHASE_2p6_WORK_LOG.md (сперва создать)
*Здесь поправить / перенести.


---

## 📋 План экспериментов Фаза 2.6 (Architecture & Refinement)

### 🛠️ Этап 0: Техническая подготовка (Prerequisites)
*Перед запуском новых опытов необходимо внести исправления в код (о которых мы договорились ранее), чтобы данные были корректными.*

1.  **Fix NaN in FFT:** [x] Скорректировать индексы выборки для `Stride`, чтобы не подавать в сеть пустые значения (NaN) из начала окна Фурье. (Выполнено в рамках Фазы 2.5 fix)
2.  **Fix Raw Snapshot:** [x] Для `raw` данных в режиме `snapshot` подавать конкатенацию `[Start_32_points + End_32_points]` (итого 64), а не 2 точки. (Выполнено в рамках Фазы 2.5 fix)
3.  **Adaptive Pooling:** [ ] Добавить `AdaptiveAvgPool1d` в модели, чтобы они "съедали" вход любой длины без ошибок (вроде бы есть).
4.  **PhysicsKAN Norm:** [ ] Добавить `BatchNorm` в физические слои (вроде бы есть).
5.  **Запуск моделей:** [ ] Сейчас есть run_phase2_5_all.py, он был нацелен на 2.5, но вроде бы почти полностью подходит для запуска новых моделей. Решить, оставить его и улучшить (и чуть поменять имя), или сделать подобный новый.

---

### [] Exp 2.6.1: Калибровка "Чистовиков" (Clean Baseline)
**Зачем:** Мы исправили баги в датасете. Нам нужно заново прогнать лучшие модели Фазы 2.5, чтобы получить честную "базовую линию" для сравнения с новыми архитектурами.

*   **Цель:** Получить эталонные метрики F1 на исправленном коде для всех 6 типов моделей.
*   **Конфигурация входа (Best of 2.5):**
    1.  `Phase Polar` + `Stride`
    2.  `Symmetric Polar` + `Snapshot`
*   **Модели:** Все 6 (SimpleCNN, ResNet1D, ConvKAN, SimpleMLP, SimpleKAN, PhysicsKAN) на всех уровнях сложности.

---

### [] Exp 2.6.2: Иерархическая обработка (Hierarchical Processing)
**Зачем:** Унификация подхода к обработке многоканальных сигналов. Текущие модели "сваливают" все каналы в кучу на первом слое, что нефизично. Мы внедряем 3-этапную иерархию для **ВСЕХ** типов архитектур (CNN, KAN, MLP).

*   **Концепция (3-Stage Hierarchy):**
    1.  **Independent Stage:** Каждый канал обрабатывается своей весовой функцией (Depthwise / Groups=InChannels). Модель учит "особенности" каждого сигнала (АЧХ, форму).
    2.  **Physical Group Stage:** Объединение в физические группы (Токи, Напряжения). Групповая свертка (Groups=2). Модель учит взаимодействие внутри типа сигнала.
    3.  **Global Stage:** Финальное смешивание всех признаков (Global Backbone).
*   **Реализованные модели:**
    *   `HierarchicalCNN`: Stem + CNN Backbone.
    *   `HierarchicalKAN`: Stem + KAN Backbone (сплайны).
    *   `HierarchicalMLP`: Реализован через Pointwise Conv1d (kernel=1) с сохранением той же иерархии ствола.
*   **Конфигурация:** Лучшие параметры Exp 2.6.1.
*   **Ожидание:** Повышение интерпретируемости (можно смотреть веса отдельных фаз), улучшение сходимости за счет структурирования задачи.

---

### [] Exp 2.6.3: Гранулярность меток (Target Granularity)
*(Перенесено из Фазы 2.5, Exp 2.5.6)*
**Зачем:** Проверка гипотезы: нужно ли нам учить сеть различать 12 типов КЗ, если для отключения выключателя достаточно знать факт аварии?

*   **Вариант А:** `target_level = 'base_labels'` (4 класса: Norm, 1-фазное, 2-фазное, 3-фазное).
*   **Вариант Б:** `target_level = 'full'` (как сейчас).
*   **Модели:** Лучшие из 2.6.1.
*   **Анализ:** Сравнить, насколько быстрее и точнее учится сеть на 4 классах. Посмотреть `Hierarchical Accuracy` (обучаем на Full, но проверяем как Base).

---

### [] Exp 2.6.4: Гибридная модель (Hybrid Architecture)
**Зачем:** Спектры (Features) точны, но инертны. Сырые данные (Raw) быстры, но шумны. Объединим их.

*   **Архитектура:** Двухголовая сеть (Two-Headed Model).
    *   **Ветка 1 (Fast):** Принимает `Raw Snapshot` (начало и конец окна, 64 точки). Легкая CNN (2 слоя). Ловит резкие фронты.
    *   **Ветка 2 (Precise):** Принимает `Phase Polar Stride` (спектральные признаки). Основная сеть.
    *   **Head:** Конкатенация векторов -> MLP -> Классы.
*   **Ожидание:** Повышение точности на сложных переходных процессах.

---

### [] Exp 2.6.5: Оптимизация гармоник (Input Optimization)
**Зачем:** 9 гармоник — это тяжело. Проверим, сколько реально нужно.

*   **Варианты:**
    *   Только 1-я гармоника (фундаментальная).
    *   1-я + 3-я + 5-я.
    *   Все 9 (Baseline).
*   **Цель:** Оценить возможность сокращения входных данных без существенной потери качества.

---

### [] Exp 2.6.6: Эксперименты с KAN-специфичными функциями (Pruning & Physics)
*(Перенесено из Фазы 2.5, Exp 2.5.7)*

**[] Exp 2.6.6.1: KAN с арифметическими слоями (PhysicsKAN)**
*   **Цель:** Оценить вклад `MultiplicationLayer` и `DivisionLayer`, разработанных в конце Фазы 2.5.

**[] Exp 2.6.6.2: Детальное прореживание KAN (Pruning)**
*   **Цель:** Попробовать поудалять связи (edge pruning) в обученных KAN моделях, чтобы оценить, сколько из них можно оставить без потери качества.
*   **Сценарии:**
    1.  На "полном" наборе меток (`target_level='full'`).
    2.  На упрощённом наборе меток (`target_level='base_labels'`, 4 класса).
*   **Модель:** Лучшая конфигурация KAN из предыдущих этапов.

---

### [] Exp 2.6.7: Интерпретация и Визуализация
*(Перенесено из Фазы 2.5, Exp 2.5.8)*

**Зачем:** KAN ценны своей интерпретируемостью. Нам нужно визуализировать, что именно выучила сеть.

1.  **Интерпретация KAN (`notebooks/020_kan_interpretability.ipynb`):**
    *   Обновить ноутбук для визуализации сплайнов и карт важности признаков, используя лучшие модели KAN из этой фазы.
    *   Попытаться визуализировать "вклад" арифметических слоев в `PhysicsKAN` (какие комбинации U и I создаёт сеть).

---

## 📝 Резюме по изменениям кода (To-Do List)

1.  **Dataset:**
    *   [x] Переписать логику `__getitem__` для корректного `fft stride` (сдвиг старта) - *проверена актуальность (реализовано)*.
    *   [x] Переписать логику `raw snapshot` (concat start+end) - *проверена актуальность (реализовано)*.
    *   [ ] (Опционально) Реализовать `WeightedRandomSampler` для DataLoader (перенесено из 2.5).
2.  **Models:**
    *   [x] Добавить `AdaptiveAvgPool1d` (присутствует в CNN моделях).
    *   [x] Создать версию моделей `_Depthwise` (для Exp 2.6.2).
    *   [ ] Создать класс `HybridModel` (принимает словарь входов: raw и features).
    *   [x] Исправить `PhysicsKAN` (добавить BatchNorm).
3.  **Runner:**
    *   [x] Адаптировать конфиги под новые типы моделей (добавлена регистрация в Runner).
