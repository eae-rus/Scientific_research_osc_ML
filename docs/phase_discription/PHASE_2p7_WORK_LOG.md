# Лог работы по Фазе 2.7 (Высококачественные эксперименты для статей)

Ведётся в обратном хронологическом порядке.

---

## 11 мая 2026 (ночь) — GPU мониторинг, auto-retry OOM, миграция путей

### Выполнено:

1. **GPU мониторинг и логирование устройства** (см. `run_phase2_7_all.py`)
   - Добавлены функции `get_gpu_memory_info()` и `print_gpu_memory()` 
   - В начале `main()` выводится: устройство (cuda/cpu), имя GPU, состояние памяти
   - Перед каждым экспериментом: batch_size (train/val) + текущее состояние GPU

2. **Auto-retry при OOM с уменьшением batch_size** (до 3 повторов)
   - При OOM в `run_single_experiment()` — автоматически уменьшается batch:
     - Сначала `val_batch_size // 2` (он обычно виноват, т.к. намного больше)
     - Если `val_bs <= train_bs`, уменьшаются оба
   - Пересоздаётся runner (модель с нуля, чтобы гарантировать чистое состояние)
   - Подробное логирование: занято/всего МБ, старый→новый batch_size
   - Только после исчерпания всех попыток — OOM пробрасывается в main()
   - Константа `MAX_OOM_RETRIES = 3`

3. **Миграция `norm_coef_all_v1.4.csv`** из `raw_data/` в `data/`
   - Обновлены 17 файлов (скрипты, тесты, визуализация, dataset_manager)
   - `dataset_manager.py`: обновлён порядок fallback-поиска (data/ вместо raw_data/)
   - Подготовка к переносу пакета коллеге (портабельность)

4. **Миграция `overvoltage_report_T1_with_com_v1.7.csv`**
   - `real_ozz_split.py` уже ссылался на `data/real_OZZ/` — корректно
   - `003_analisis.ipynb` — обновлён путь в коде на `data/real_OZZ/`

5. **Исправление бага**: `total_mem` → `total_memory` (правильный атрибут PyTorch)

### Тесты: 536 passed, 1 skipped (все проходят)

---

## 11 мая 2026 (вечер) — OOM защита, система чекпоинтов, оценка и тесты

### Выполнено:

1. **Защита от переполнения GPU памяти (OOM)**
   - `runner.py`: добавлен метод `cleanup()` — переносит модель на CPU, удаляет model/optimizer/criterion, вызывает `torch.cuda.empty_cache()` + `gc.collect()`
   - `run_phase2_7_all.py`: `runner.cleanup()` вызывается в `finally` блоке после каждого эксперимента
   - `torch.cuda.OutOfMemoryError` перехватывается отдельно — принудительная очистка + пропуск (не крашит цикл)
   - После каждого эксперимента: `del train_loader, val_loader, train_ds, val_ds` + `gc.collect()`

2. **Система last/best/final моделей** (см. `osc_tools/ml/runner.py`)
   - `last_model.pt` — сохраняется после **каждой** эпохи (перезаписывается), позволяет восстановиться при сбое
   - `best_model.pt` — как и раньше, по минимуму val_loss
   - `final_model.pt` — маркер завершения обучения, сохраняется только в конце
   - После сохранения `final_model.pt` файл `last_model.pt` автоматически удаляется
   - Проверка `skip_existing` проверяет именно `final_model.pt` → прерванное обучение перезапустится

3. **Интеграция OZZ эксперимента** (см. `run_phase2_7_all.py`)
   - Добавлен параметр `target_level` (base/ozz), прокинут через всю цепочку
   - Конфигурация `OZZ_EXPERIMENT_CONFIG`: phase_polar, stride/none, heavy, все модели paper1
   - CLI: `--target_level ozz`, `--include_ozz`
   - Имя эксперимента OZZ получает суффикс `_ozz` (напр. `Exp_2.7_f1_s0_SimpleMLP_heavy_phase_polar_stride_ozz`)

4. **Обновление парсера** (см. `scripts/evaluation/_core/config_resolvers.py`)
   - Regex обновлён: поддержка двухуровневых ID (`2.7`)
   - Парсинг `fold` и `seed_idx` из формата `_f{N}_s{M}_`
   - Новые feature_modes: `phase_complex`, `symmetric_polar`, `alpha_beta`
   - Sampling `none` → `Dense`

5. **Агрегация по fold/seed** (см. `scripts/evaluation/aggregate_reports.py`)
   - Поля `Fold` и `SeedIdx` добавлены в сводную таблицу
   - Новая функция `aggregate_by_fold_seed()` — группировка по конфигурации, расчёт mean ± std
   - Результат в `summary_aggregated.csv`

6. **Тесты** — 30 тестов, все проходят:
   - `tests/unit/test_config_resolvers.py` — 19 тестов (парсинг имён 2.7 и 2.6, fold/seed, feature_modes, OZZ, обратная совместимость)
   - `tests/unit/test_aggregate_fold_seed.py` — 7 тестов (mean±std, подсчёт N_runs, граничные случаи)
   - `tests/unit/test_ml_runner.py` — 4 теста (init, train, cleanup, last_model.pt)

7. **Прочие доработки**
   - PatchTST/TimesNet: убрано `fixed_complexity` из TODO
   - FIXME о precomputed для fold → заменён на NOTE (on-the-fly достаточен)

### Первый тестовый прогон (1 эпоха, fold 1, seed 0):
- Всего ~150 конфигураций, большинство обучились успешно
- OOM ошибки: ConvKAN_heavy_raw_none, ConvKAN (все уровни) на phase_polar_none, все Physics*KAN на phase_polar_none
- Причина: 8 ГБ GPU недостаточно для KAN-моделей с dense sampling (320 точек × много каналов)
- На stride/snapshot те же модели обучаются без проблем

---

## 11 мая 2026 — Инициализация Фазы 2.7

### Выполнено:

1. **5-кратная перекрёстная проверка (K-Fold CV)**
   - Реализован метод `DatasetManager.generate_cv_splits()` — стратифицированное разбиение
     по осциллограммам с учётом комбинаций меток (ML_1, ML_2, ML_3)
   - Сохранение в `data/ml_datasets/cv_splits.json` для воспроизводимости
   - Баланс: 198 тестовых / 792 обучающих файлов в каждом fold-е
   - Классы равномерно распределены: ML1≈27-28, ML2≈10-11, ML3≈20-21 на fold
   - Добавлены `load_cv_splits()` и `load_fold_data()` для удобной загрузки

2. **Новый скрипт `run_phase2_7_all.py`**
   - Полная переработка скрипта экспериментов с 5-ю вложенными циклами
   - Поддержка CLI для параллельного запуска на сервере
   - Поддержка ручного запуска через IDE (константы)
   - Автоматическая фильтрация несовместимых комбинаций модель/данные
   - Разделение на контексты статей (paper1: модели, paper2: данные)

3. **Множественные seeds** 
   - 5 seeds с простыми числами в качестве смещений: [0, 7919, 15731, 23497, 31607]
   - Обеспечивают максимальное различие инициализаций

4. **Поддержка rPhysicsKAN в runner**
   - Добавлен импорт и инстанцирование `rPhysicsKAN` в `ExperimentRunner`

5. **Тестовый прогон**
   - Успешно обучена модель `SimpleMLP_light_raw_stride` на Fold 1, Seed 0, 1 эпоха
   - Подтверждена корректность: загрузка данных, создание индексов, обучение, валидация,
     сохранение чекпоинтов

### Технические решения:

- **Валидационная выборка**: решено НЕ выделять отдельную (train/val/test),
  т.к. при 51 файле ML_2 и 5-fold CV это даёт ~10 файлов ML_2 в тесте — 
  дальнейшее дробление нецелесообразно.
- **Аугментация**: всегда включена (стандартный набор из Фазы 2.5).
- **Взвешивание**: BCEWithLogitsLoss с pos_weight по частоте событий.
- **Обратная совместимость**: все изменения аддитивны, старые скрипты не затронуты.

### TODO на следующий шаг:
- [ ] Запуск полного цикла тестов (все комбинации, epochs=1) для выявления ошибок
- [ ] Реализация PatchTST и TimesNet моделей
- [ ] Скрипт агрегации результатов по фолдам/seeds (mean ± std)
- [ ] Предрасчёт признаков для каждого fold-а (ускорение валидации)
