# План Фазы 2.5: Углубленная оптимизация данных и архитектур (Data-Centric Refinement)

-> **Статус:** Активна
> **Примечание:** Размеченные данные для PDR (PDR / РНМ) в настоящий момент отсутствуют — помечено как TODO. Если встретите упоминания про PDR в планах/коде, пока можно их игнорировать.
> **Цель:** Устранение ограничений Фазы 2, внедрение продвинутого пайплайна данных (Sliding Window, Downsampling), адаптация под сложную структуру сигналов (СШ/КЛ, фазные/линейные) и переход к Multi-Label классификации.

## 🎯 Основные цели фазы

1.  **Data Pipeline 2.0:** Переход от "одного окна с начала файла" к полноценному сэмплированию данных (Random Sliding Window) по всей длине осциллограммы.
2.  **Smart Features:** Реализация гибкой логики выбора сигналов (токи, напряжения шин/линий) и переход к физически интерпретируемым признакам (Модуль/Угол относительно фазы А базисного сигнала).
3.  **Optimization:** Сравнение стратегий прореживания данных (Stride vs Snapshot) для ускорения KAN.
4.  **Target Granularity:** Обучение на двух уровнях детализации: "4 базовых класса" и "Полный набор событий" (Multi-label).
5.  **Data Augmentation:** Применение аугментации временных рядов для борьбы с переобучением и увеличения вариативности обучающей выборки (добавление шума, временные сдвиги, масштабирование амплитуды в физически допустимых пределах).
6.  **Improved Reporting:** Внедрение расширенной системы логирования и статистики обучения с автоматической генерацией отчётов для последующего анализа и формирования научных выводов (с учётом длительного обучения моделей).
7.  **Class Balancing:** Внедрение механизмов учёта несбалансированности классов (нормальных режимов значительно больше, чем аварий) через взвешивание в функции потерь или метриках. Реализация иерархического подхода для обработки неизвестных подклассов аварий (если встречается ML_3_2_1_1, а в обучении был только ML_3_2_1 — классифицировать как родительский класс).
8.  **KAN Arithmetic Layers:** Исследование добавления специализированных слоёв для физически значимых арифметических операций (умножение для расчёта мощности, деление для сопротивления/проводимости) с корректной обработкой граничных случаев (деление на значения близкие к нулю). Цель — повышение интерпретируемости KAN через обучаемые физические преобразования.


---

## 📅 Детальный план работ

### Этап 1: Рефакторинг работы с данными (Data Engineering)
*Самый важный этап. Мусор на входе = мусор на выходе.*

1.  **Аудит и селектор сигналов:**
    *   **Проблема:** В данных "зоопарк" колонок: `IA, IB, IC, IN`, `UA BB` (шины), `UA CL` (линия), линейные `UAB` и т.д.
    *   [x] **Задача:** Реализовать умный селектор в `OscillogramDataset`. (см. `osc_tools/ml/dataset.py`)
        *   Если есть фазные `UA`, брать их (причём они могут быть 0 все - значит нет, а могут быть шум, то есть есть. Надо подумать, как заносятся).
        *   Если есть только линейные `UAB`, использовать их (но помнить, что $U_0$ из них не получить).
        *   Если нет СШ (BB) - то пробовать работать по КЛ (CL).
        *   А если и их нет - то базисным является ток фазы А.
        *   Входной тензор модели должен быть динамическим или фиксированным максимальным (с заполнением нулями отсутствующих каналов). Разобраться с возможностью определения присутствующих сигналов.
    *   [x] **Задача:** Реализовать логику расчета симметричных составляющих для линейных напряжений (только $U_1, U_2$). (см. `osc_tools/features/phasor.py`)
    *   [x] **Задача:** Добавить поддержку каналов нулевой последовательности (In, Un) в селектор и расчет признаков. (см. `osc_tools/ml/dataset.py`)

2.  **Глобальная нормализация:**
    *   [x] **Задача:** Написать скрипт `scripts/calculate_dataset_stats.py`.
    *   [x] Проход по всему Train-сету и расчет `Mean` и `Std` для каждого типа канала (Токи, Напряжения).
    *   [x] Сохранение в `stats.json`.
    *   Внедрение загрузки этих статистик в `OscillogramDataset` для Z-нормализации и проверки физической нормализации входных данных (Если они прям сильно выходят за диапазоны - значит не были нормированы от исходных значений).

3.  **Sliding Window & Random Sampling:**
    *   [x] **Задача:** Модифицировать `Dataset`. Вместо `start_idx=0` реализовать логику: (см. `osc_tools/ml/dataset.py`)
        *   *Train:* Случайный выбор окна длиной `window_size` в пределах файла. В связи с этим решить с объёмом обучающего набора (Я раньше фиксировал количество примеров в одной эпохе и прост случайные брал осциллограммы и в них рандомные куски)
        *   *Val/Test:* Скользящее окно с шагом (например, 50% перекрытия) для покрытия всей осциллограммы.
    *   **Параметр окна:** Установить базовый размер окна **200 мс** (320 точек при 1600 Гц). Это физически обоснованное время.

4.  **Аугментация данных (Data Augmentation):**
    *   **Проблема:** Недостаточное количество реальных осциллограмм аварийных режимов, риск переобучения.
    *   [x] **Задача:** Реализовать модуль аугментации временных рядов с физически обоснованными преобразованиями. (см. `osc_tools/ml/augmentation.py`)
        *   **Gaussian Noise:** Добавление гауссова шума (SNR = 30-50 dB) для имитации погрешностей измерения.
        *   **Time Jitter:** Случайный временной сдвиг окна (реализовано через Random Sampling в `OscillogramDataset`).
        *   **Scaling:** Масштабирование амплитуды (отдельно для токов и напряжений).
        *   **Phase Shuffling:** Перестановка фаз (A->B->C) для инвариантности к фазе.
        *   **Drop Channel:** Имитация потери канала.
    *   **Важно:** Аугментация применяется только к Train-набору. Val/Test остаются без изменений.
    *   [x] **Код:** Найти существующий код аугментации (возможно в старых версиях или в комментариях) и адаптировать под новый пайплайн. (Интегрировано в `OscillogramDataset`)

### Этап 2: Стратегии формирования входа (Input Strategies)
*Здесь мы решаем проблему скорости KAN и физического смысла.*

1.  **Переход на Модуль/Угол:**
    *   [x] **Задача:** Заменить `Re/Im` на `Magnitude/Angle`.
    *   **Важно:** Фаза (Angle) сама по себе бесполезна, так как зависит от момента начала окна.
    *   [x] **Решение:** Считать разность фаз относительно **Напряжения Фазы А** (или линейного напряжения, или Тока А, если напряжения нет). $\phi_{rel} = \phi_{x} - \phi_{Ua}$. Это делает признак инвариантным к сдвигу во времени.

2.  **Реализация 3-х стратегий сэмплирования (для окна 200 мс / 320 точек):**
    *   [x] **Стратегия A (Dense/Raw):** Все 320 точек сырого сигнала (или 320 рассчитанных векторов RMS/Phase). *Самая тяжелая.*
    *   [x] **Стратегия B (Strided/Downsampled):** Расчет признаков (RMS/SymComp) с шагом (stride) **16 точек**.
        *   Итоговый размер: $320 / 16 = 20$ временных отсчетов.
        *   Это идеальный баланс для KAN (мало точек, много смысла).
    *   [x] **Стратегия C (Snapshot/Sparse):** Взятие только двух точек: $t_{current}$ и $t_{start}$ (начало окна, -200мс).
        *   Итоговый размер: 2 временных отсчета.
        *   Проверка гипотезы: "Достаточно знать, что было 'до' и что 'сейчас', чтобы понять тип аварии".

3.  **Сравнение подходов к данным:**
    *   **Задача:** Провести сравнительный анализ моделей, обученных на:
        *   **Вариант 1:** Все доступные аналоговые сигналы (как есть в файле).
        *   **Вариант 2:** Умный селектор (стандартизированные 6 каналов + восстановленные).
    *   Цель: Понять, дает ли избыточность сигналов прирост качества или вносит шум.

### Этап 3: Работа с целевыми метками (Targets)

1.  **Multi-Label подход:**
    *   [x] **Задача:** Изменить кодирование `y`. Вместо одного числа (класс 0..N) выдавать вектор бинарных флагов $[0, 1, 0, 1, ...]$.
    *   [x] Использовать `BCEWithLogitsLoss`.

2.  **Два уровня детализации:**
    *   [x] **Level 1 (Base):** 4 класса.
        *   `Normal` (Нет флагов)
        *   `ML_1` (Рабочая коммутация)
        *   `ML_2` (Аномалия)
        *   `ML_3` (Авария)
        *   *Примечание:* Это требует маппинга исходных `ML_...` колонок в эти 4 категории. И то есть сигнал `ML_1_1` относится к рабочей коммутации, важна только первая цифра.
    *   **Level 2 (Full):** Все уникальные флаги.

3.  **Взвешивание классов и метрики:**
    *   **Проблема:** Критическая несбалансированность датасета (Normal режимы ~70-80%, аварийные ML_3_* ~5-10%).
    *   [x] **Задача:** Реализовать механизмы учёта несбалансированности:
        *   [x] **Class Weights:** Рассчитать веса классов `w_i = (N - N_i) / N_i` и внедрить в `BCEWithLogitsLoss(pos_weight=weights)`. (см. `osc_tools/ml/class_weights.py`)
        *   **Balanced Sampling:** Рассмотреть `WeightedRandomSampler` для более частого сэмплирования редких классов.
        *   **Метрики:** Переход от общей Accuracy к **Balanced Accuracy**, **F1-macro**, **Precision/Recall per class**.
    *   [x] **Код:** Реализовано в `osc_tools/ml/class_weights.py` и интегрировано в `ExperimentRunner`.

4.  **Иерархическая обработка неизвестных подклассов:**
    *   **Проблема:** В Test/Val может встретиться уникальный подкласс аварии (например, `ML_3_2_1_1`), которого не было в Train.
    *   **Решение:** Использовать иерархическую структуру меток:
        *   Если модель видит `ML_3_2_1_1`, но обучалась только на `ML_3_2_1` — считать его родительским классом.
        *   Реализовать функцию `map_to_parent_class(label, trained_labels)` для валидации.
    *   **Метрика:** Добавить Hierarchical Precision (учёт частичного совпадения на уровне родительских классов).

### Этап 4: Адаптация моделей

1.  **Обновление входных слоев:**
    *   Модели должны принимать динамическое количество каналов (в зависимости от файла конфигурации `input_channels`).
    *   Для KAN/MLP: Учет изменения размера входа при смене стратегии (20 точек vs 320 точек).

2.  **Обновление выходных слоёв (Heads):**
    *   Реализовать `MultiHead` или просто конфигурируемый выходной слой.
    *   Head 1: 4 нейрона (для Base классов).
    *   Head 2: N нейронов (для всех флагов).
    *   *Гипотеза:* Можно учить одну сеть с двумя головами одновременно (Multi-task learning).

3.  **KAN с арифметическими операциями (KAN Arithmetic Layers):**
    *   **Мотивация:** Органы РЗА работают с физическими величинами: мощность ($S = U \cdot I$), сопротивление ($Z = U / I$), проводимость ($Y = I / U$).
    *   [x] **Задача:** Разработать специализированные слои для KAN: (см. `osc_tools/ml/kan_conv/arithmetic.py`)
        *   [x] **MultiplicationLayer:** Поэлементное умножение двух входных векторов (для расчёта мощности).
        *   [x] **DivisionLayer:** Деление с обработкой нулевых знаменателей (используется Clipping).
        *   **Интеграция:** Вставить эти слои между KAN-блоками, чтобы сеть могла обучать физические преобразования.
    *   **Пример архитектуры:**
        ```
        Input (U, I) -> KAN_Block -> [U_feat, I_feat] -> MultiplicationLayer -> Power_feat -> KAN_Block -> Output
                                                       -> DivisionLayer -> Impedance_feat -> ...
        ```
    *   **Интерпретируемость:** После обучения можно визуализировать, какие комбинации $U$ и $I$ создаёт сеть, и сравнить с классическими формулами РЗА.
    *   [x] **Код:** Реализовано в `osc_tools/ml/kan_conv/arithmetic.py`.


---

### Этап 4.5: Углубление архитектур (Model Scaling & Tuning)

**Цель:** Уйти от "игрушечных" конфигураций к более глубоким моделям (Deep Learning), способным выучивать сложные иерархические зависимости, но надо не забывать, что эксперименты должны быть сравнимы, поэтому предлагаю ввести 3 уровня сложности по ширине первого слоя моделей, ну и глубине, хотя это не так критично (лёгкая - будет для ВСЕХ точек, средняя - для прореженного окна с шагом 16 например, и тяжёлая - для "двух точек", ведь там мало данных).

1.  **Параметризация глубины (Dynamic Depth):**
    *   **Проблема:** Сейчас количество слоев часто "захардкожено" (например, `block1 -> block2 -> block3`).
    *   [x] **Задача:** Переписать конструкторы моделей (`SimpleCNN`, `ConvKAN`, `SimpleMLP`) так, чтобы они принимали списки параметров слоев. (см. [osc_tools/ml/models/baseline.py](osc_tools/ml/models/baseline.py) и [osc_tools/ml/models/kan.py](osc_tools/ml/models/kan.py))
        *   *Пример конфига:* `hidden_sizes: [256, 128, 64, 32]` (для MLP) или `channels: [16, 32, 64, 128, 256]` (для CNN).
        *   Это позволит менять глубину через `yaml` конфиг без правки кода.

2.  **Deep MLP (4-5 слоев):**
    *   **Концепция:** "Пирамидальная" структура для постепенного сжатия информации.
    *   [x] **Архитектура:** `Input -> Linear(512) -> ReLU -> BN -> Linear(256) -> ... -> Linear(64) -> Output`. (Реализовано в [osc_tools/ml/models/baseline.py](osc_tools/ml/models/baseline.py))
    *   **Важно:** Обязательное использование `Dropout` (0.3-0.5) между слоями, так как полносвязные сети очень склонны к переобучению при увеличении глубины.

3.  **Deep CNN (5-7 блоков):**
    *   **Концепция:** Увеличение рецептивного полей. Чем глубже сеть, тем более длинные временные зависимости она видит.
    *   [x] **Архитектура:**
        *   Block 1-2: Выделение резких фронтов и шумов.
        *   Block 3-4: Выделение периодов и гармоник.
        *   Block 5: Выделение паттернов огибающей (модуляции).
    *   [x] **Задача:** Реализовать `DeepCNN` с 5-7 сверточными блоками. Использовать `Kernel Size` = 3 или 5, постепенно уменьшая размерность временем через `MaxPool` и увеличивая количество каналов (до 128 или 256). (Реализовано гибко в [osc_tools/ml/models/baseline.py](osc_tools/ml/models/baseline.py))

4.  **Deep ConvKAN (3-5 блоков):**
    *   **Сложность:** KAN вычислительно тяжелее, поэтому просто так сделать 10 слоев нельзя (будет очень медленно).
    *   **Стратегия:** Сделать сеть глубже, но "уже" (меньше каналов).
    *   [x] **Задача:** Реализовать конфигурацию с 4-5 блоками `KANConv1d`. (Реализовано в [osc_tools/ml/models/kan.py](osc_tools/ml/models/kan.py))
        *   Пример: `8 -> 16 -> 24 -> 32` каналов.
        *   Попробовать уменьшать `grid_size` в глубоких слоях (например, 1-й слой `grid=10`, глубокие слои `grid=3`), так как глубокие признаки часто более абстрактны и сглажены.

5.  **Регуляризация для глубоких сетей:**
    *   Так как мы увеличиваем емкость моделей (Capacity), риск переобучения растет.
    *   [x] **Задача:** Убедиться, что во всех моделях корректно расставлены `BatchNorm1d` (перед активацией) и `Dropout` (после активации).
    *   [x] **Задача:** Добавить поддержку `Weight Decay` в оптимизатор (уже настроено в `ExperimentRunner`).

**Задача:** [x] После этого обновить описание моделей на следующем шаге (5), ведь мы как минимум добавлии 3 уровня сложности моделей, а значит, даже если писал о "все" - то идея поменяется, да и сейчас это никак не описано.

---

### Этап 5: Эксперименты (Experiments)### Этап 5: Детальный план экспериментов (Experimentation Protocol)

**Общий подход:** Эксперименты будут проводиться итеративно, с последовательным введением новых переменных. Каждый эксперимент будет включать обучение на наборе отобранных моделей, с фиксацией ключевых метрик и условий. Приоритет отдается воспроизведению (с улучшениями) наиболее показательных экспериментов Фазы 2, а также исследованию новых возможностей Фазы 2.5.

**Базовые условия для всех экспериментов (если не указано иное):**
*   **Длина окна:** `window_size = 320` точек (200 мс).
*   **Сэмплирование в Train:** `Random Sliding Window` (случайное окно в пределах файла).
*   **Сэмплирование в Val/Test:** `Sliding Window` (с шагом 50% перекрытия) для полного покрытия.
*   **Нормализация:** Z-нормализация по глобальным `Mean/Std` всего датасета.
*   **Количество эпох:** `epochs = 30-50` (или до сходимости/переобучения, контролируемое по Val Loss).
*   **Количество примеров в эпохе:** `count = 1000-10000` (случайный выбор окна из случайной осциллограммы из обучающих наборов).
*   **Оптимизатор:** Adam, `learning_rate = 0.001`.
*   **Устройство:** `cuda` (если доступно), инференс на `cpu` для оценки скорости.
*   **Логирование:** Расширенная система логирования (`metrics.jsonl`, `config.json`), периодические чекпоинты.
*   **Метрики:** `Val Loss`, `Balanced Accuracy`, `F1-macro`, `Precision/Recall per class`.
*   **Baseline:** `SimpleCNN` (как наиболее сбалансированная модель из Фазы 2).

**Модели для тестирования (каждая тестируется на 3-х уровнях сложности: Light, Medium, Heavy):**
-   `SimpleMLP` (с адаптированным входом под текущую стратегию окна)
-   `SimpleCNN` (с глубиной 3-7 блоков)
-   `ConvKAN` (с глубиной 2-5 блоков и переменным `grid_size`)
-   `ResNet1D`
    - Light: 3 блока (Layers 1-1-1), Base Filters 16
    - Medium: 4 блока (Layers 2-2-2-2), Base Filters 32
    - Heavy: 4 блока (Layers 3-4-6-3), Base Filters 64
-   `SimpleKAN`
    - Light: Hidden [64, 32], Grid 3
    - Medium: Hidden [128, 64, 32], Grid 5
    - Heavy: Hidden [256, 128, 64, 32], Grid 5
-   `PhysicsKAN` (аналогично ConvKAN, плюс арифметические слои)


---

#### 5.1. Калибровка пайплайна и Базовые сравнения (Baseline Refinement)

**Цель:** Установить новый, более качественный Baseline, убедиться в корректной работе нового пайплайна данных (Sliding Window, Normalization, Smart Selector).

**Exp 2.5.1.0: Валидация "Умного селектора" и Исходные сигналы (Raw)**
*   **Цель:** Проверить, как новый "Умный селектор" каналов (с приоритетами BB/CL/Линейные) влияет на качество обучения на базовых сырых данных. Убедиться, что все каналы (IA,IB,IC,IN, UA BB,UB BB,UC BB,UN BB,UAB BB,UBC BB,UCA BB, UA CL,UB CL,UC CL,UN CL,UAB CL,UBC CL,UCA CL) корректно обрабатываются.
*   **Модели:** все.
*   **Вход:**
    *   `feature_mode = 'raw'` (все доступные каналы, отобранные `Smart Selector`).
    *   `sampling_strategy = 'dense'` (все 320 точек).
*   **Целевые метки:** `target_level = 'base_labels`, 4 обобщённых класса (Multi-Label).
*   **Доп. условия:** Без аугментации, без взвешивания классов.
*   **Ожидание:** Повышение Accuracy/F1 по сравнению с Phase 2 за счет улучшенного сэмплирования и более релевантных входных сигналов.

**Exp 2.5.1.1: Сравнение сбалансированных классов (Full Multi-Label)**
*   **Цель:** Оценить влияние взвешивания классов на метрики для редких событий.
*   **Модели:** все.
*   **Вход:** `feature_mode = 'raw'`, `sampling_strategy = 'dense'`.
*   **Целевые метки:** `target_level = 'base_labels'` (Multi-Label).
*   **Переменная:** `use_pos_weight = True` (с рассчитанными весами).
*   **Ожидание:** Улучшение `Recall` и `F1` для редких классов, возможно, небольшое падение общей `Balanced Accuracy`.

**Exp 2.5.1.2: Оценка влияния аугментации**
*   **Цель:** Проверить, как аугментация влияет на обобщающую способность и стабильность обучения.
*   **Модели:** все.
*   **Вход:** `feature_mode = 'raw'`, `sampling_strategy = 'dense'`.
*   **Целевые метки:** `target_level = 'base_labels'`.
*   **Переменная:** `augmentation_config = {...}` (с набором из 3-4 преобразований).
*   **Доп. условия:** Со взвешиванием классов (`use_pos_weight = True`).
*   **Ожидание:** Снижение переобучения (уменьшение разрыва Train/Val Loss), повышение `Balanced Accuracy` на валидации.

---

#### 5.2. Исследование стратегий прореживания данных (Data Downsampling)

**Цель:** Найти оптимальный баланс между детализацией входных данных и вычислительной сложностью, используя новые стратегии сэмплирования.

**Exp 2.5.2.1: Стратегия B ("Strided")**
*   **Цель:** Оценить эффективность `strided` сэмплирования признаков (20 временных отсчетов вместо 320) для `ConvKAN` и `SimpleCNN`.
*   **Модели:** все.
*   **Вход:**
    *   `feature_mode = 'symmetric'` (симметричные составляющие).
    *   `sampling_strategy = 'strided'` (шаг 16, 32 точек).
    *   `polar_features = True` (Magnitude/Angle).
*   **Целевые метки:** `target_level = 'base_labels'`.
*   **Доп. условия:** С аугментацией, со взвешиванием классов.
*   **Ожидание:** Значительное ускорение обучения и инференса, при сохранении (или небольшом падении) качества.

**Exp 2.5.2.2: Стратегия C ("Snapshot")**
*   **Цель:** Проверить, достаточно ли всего 2-х "снимков" состояния (начало и конец окна) для классификации аварии.
*   **Модели:** Все.
*   **Вход:**
    *   `feature_mode = 'symmetric'` (симметричные составляющие).
    *   `sampling_strategy = 'snapshot'` (2 точки).
    *   `polar_features = True` (Magnitude/Angle).
*   **Целевые метки:** `target_level = 'base_labels'`.
*   **Доп. условия:** С аугментацией, со взвешиванием классов.
*   **Ожидание:** Очень высокая скорость инференса. Качество может быть снижено, но важно оценить пределы упрощения.

---

#### 5.3. Оптимизация признакового пространства (Feature Type Optimization)

**Цель:** Сравнить различные типы физически обоснованных признаков на оптимальной стратегии прореживания.

**Exp 2.5.3.1: Сравнение Mag/Angle vs Re/Im**
*   **Цель:** Установить, какая форма представления комплексных чисел (полярная или декартова) лучше воспринимается моделями.
*   **Модели:** Все.
*   **Вход:**
    *   `feature_mode = 'symmetric'` (или `complex_channels`).
    *   `sampling_strategy = 'strided'` (из Exp 2.5.2.1).
*   **Переменная:** `polar_features = True` vs `polar_features = False`.
*   **Целевые метки:** `target_level = 'base_labels'`.
*   **Доп. условия:** С аугментацией, со взвешиванием классов.
*   **Ожидание:** `Magnitude/Angle` должно дать лучший или сопоставимый результат с более высокой интерпретируемостью.

**Exp 2.5.3.2: Сравнение типов признаков (Symmetric vs Power vs Alpha-Beta)**
*   **Цель:** Определить наиболее информативный набор физических признаков.
*   **Модели:** Все.
*   **Вход:** `sampling_strategy = 'strided'`, `polar_features = True`.
*   **Переменная:** `feature_mode` (`'symmetric'`, `'power'`, `'alpha_beta'`).
*   **Целевые метки:** `target_level = 'base_labels'`.
*   **Доп. условия:** С аугментацией, со взвешиванием классов.
*   **Ожидание:** Повторение вывода Фазы 2 о превосходстве симметричных составляющих.

---

#### 5.4. Исследование уровней детализации меток (Target Granularity)

**Цель:** Оценить влияние детализации целевых меток на качество обучения и возможность иерархической классификации.

**Exp 2.5.4.1: Классификация 4-х базовых классов**
*   **Цель:** Обучить модели на упрощенном наборе из 4-х базовых классов (Normal, ML_1, ML_2, ML_3).
*   **Модели:** Все.
*   **Вход:** Оптимальный набор (из Exp 2.5.2.1 и 2.5.3.2).
*   **Целевые метки:** `target_level = 'base_labels'` (Multi-Label, 4 флага).
*   **Доп. условия:** С аугментацией, со взвешиванием классов.
*   **Ожидание:** Более высокая `Balanced Accuracy` из-за меньшей сложности задачи.

**Exp 2.5.4.2: Иерархическая оценка (Full vs Base)**
*   **Цель:** Использовать метрику `Hierarchical Accuracy` для оценки моделей, обученных на `full` уровне, по сравнению с `base` уровнем.
*   **Модели:** Все (обученная на `full` уровне).
*   **Вход:** Оптимальный набор.
*   **Целевые метки:** `target_level = 'full'`.
*   **Анализ:** Оценить `get_hierarchical_accuracy` (частичное совпадение метки) на тестовом наборе.
*   **Ожидание:** Модель, обученная на `full` уровне, должна демонстрировать хорошую `Hierarchical Accuracy`, даже если не предсказала точный подкласс.

---

#### 5.5. Эксперименты с KAN-специфичными функциями

**Цель:** Исследовать потенциал специализированных KAN-слоев для повышения интерпретируемости и эффективности.

**Exp 2.5.5.1: KAN с арифметическими слоями (PhysicsKAN)**
*   **Цель:** Оценить, улучшает ли добавление `MultiplicationLayer` и `DivisionLayer` производительность `ConvKAN` и его интерпретируемость.
*   **Модели:** `ConvKAN`, `PhysicsKAN` (на основе ConvKAN с встроенными арифметическими слоями).
*   **Вход:** Оптимальный набор (из Exp 2.5.2.1 и 2.5.3.2). Возможно, потребуется специальная подготовка каналов для этих слоев (например, разделение $U$ и $I$).
*   **Целевые метки:** `target_level = 'full'`.
*   **Доп. условия:** С аугментацией, со взвешиванием классов.
*   **Анализ:**
    *   Сравнение Accuracy/F1.
    *   Попытка интерпретировать веса арифметических слоев: какие $U$ и $I$ умножаются/делятся? Соответствует ли это формулам мощности/импеданса?

**Exp 2.5.5.2: Детальное прореживание KAN (Pruning)**
*   **Цель:** Повторить эксперименты по прунингу (из Фазы 2) на лучшей `ConvKAN` (или `PhysicsKAN`) модели, обученной на оптимальных данных.
*   **Модели:** Лучшая `ConvKAN` или `PhysicsKAN`.
*   **Вход:** Оптимальный набор.
*   **Анализ:**
    *   Как изменился процент прореживания без потери качества?
    *   Как выглядят функции активации после прунинга? Стали ли они еще "проще" и линейнее?

---

#### 5.6. Отчетность и визуализация

**Цель:** Автоматизировать анализ результатов и генерацию отчетов для научного анализа.

1.  **Скрипт `aggregate_reports.py`:**
    *   [x] Расширить функционал для сбора всех метрик (Balanced Acc, F1 per class).
    *   [x] Добавить генерацию сводных таблиц для каждого типа эксперимента (например, "Сравнение стратегий окна").
    *   [x] Внедрить генерацию `Confusion Matrix` для лучших моделей по всем классам и по базовым 4-м классам.
    *   [x] Расширить возможности построения `Learning Curves` для Multi-Label Loss.

2.  **Интерпретация KAN (`notebooks/020_kan_interpretability.ipynb`):**
    *   [ ] Обновить ноутбук для визуализации сплайнов и карт важности признаков, используя лучшие модели KAN из этой фазы.
    *   [ ] Попытаться визуализировать "вклад" арифметических слоев в `PhysicsKAN`.

---
**Важно:** На каждом шаге, после завершения экспериментов, необходимо проводить детальный анализ результатов, чтобы принимать обоснованные решения о дальнейшем ходе исследований и возможных корректировках плана.

---

## 🛠 Технические задачи (To-Do List)

### Данные (Data)
- [x] **Script:** `scripts/calculate_dataset_stats.py` — Расчет Mean/Std для нормализации.
- [x] **Code:** Обновить `OscillogramDataset`:
    - [x] Добавить `select_channels` логику (приоритет BB -> CL).
    - [x] Добавить `relative_angle` (угол относительно фазы А).
    - [x] Добавить `sampling_strategy` ('dense', 'strided', 'snapshot').
    - [x] Добавить `target_level` ('base', 'full').
- [x] **Code:** Реализовать модуль аугментации `osc_tools/ml/augmentation.py`:
    - [x] Функции: `add_gaussian_noise`, `amplitude_scaling`, и т.д..
    - [x] Интеграция в `OscillogramDataset` через параметр `augment=True` (только для train).
- [x] **Code:** Реализовать взвешивание классов:
    - [x] Интеграция `pos_weight` в `BCEWithLogitsLoss`. (см. `osc_tools/ml/class_weights.py`, интеграция в `osc_tools/ml/runner.py`)
    - [ ] Опционально: Реализовать `WeightedRandomSampler` для DataLoader.
- [x] **Code:** Реализовать иерархический маппинг меток `map_to_parent_class` в `osc_tools/core/label_utils.py`.

### Модели (Models)
- [x] **Code:** Обновить модели:
    - [x] Поддержка `BCEWithLogitsLoss` (изменить активацию выхода на Sigmoid или убрать её, если она в Loss).
- [x] **Code:** Реализовать арифметические слои для KAN в `osc_tools/ml/kan_conv/arithmetic.py`:
    - [x] `MultiplicationLayer`: Поэлементное умножение двух входов.
    - [x] `DivisionLayer`: Деление с защитой от нуля (clipping, epsilon, маскирование).
- [x] **Code:** Создать новую модель `PhysicsKAN` с встроенными арифметическими слоями.

### Обучение (Training)
- [ ] **Config:** Создать конфиги для новых экспериментов (`configs/phase2_5/exp_strided.yaml`, etc.).
- [x] **Code:** Внедрить расширенную систему логирования:
    - [x] Автоматическое сохранение метрик в `metrics.jsonl`.
    - [x] Логирование по эпохам: Train/Val Loss, Accuracy, F1-macro, Balanced Accuracy, per-class metrics.
    - [x] Сохранение конфигурации эксперимента в `config.json`.
    - [x] Периодическое сохранение чекпоинтов модели (каждые 10 эпох).
- [x] **Script:** Создать `scripts/evaluation/aggregate_reports.py` для постобработки:
    - [x] Сбор всех метрик из `metrics.jsonl`.
    - [x] Генерация сводных таблиц (CSV/Markdown).
    - [x] Автоматическая генерация графиков (Learning Curves).
- [ ] **Script:** Добавить генерацию Confusion Matrices в `aggregate_reports.py`.

---

## 📈 Ожидаемые результаты Фазы 2.5

1.  **Оптимальная модель входа:** Мы найдем минимально необходимый набор данных (например, "Стратегия B: 20 точек, Mag/Angle"), на котором KAN учится быстро и точно.
2.  **Универсальность:** Пайплайн сможет "переваривать" файлы с разным набором сигналов (есть напряжения на секции шин — хорошо, нет — работаем по датчикам на линии (КЛ)).
3.  **Физическая адекватность:** Переход к углам и модулям сделает работу сети понятной для релейщика (интерпретируемость).

## ⚠️ Риски
-   **Отсутствие фазы А:** Для расчета относительного угла нужен опорный вектор. Если $U_A$ и $I_A$ отсутствуют или зашумлены, относительный угол "поплывет". *Митигация:* Использовать фазу B или C, если А недоступна, или использовать приращения ($\Delta \phi$).
-   **Несбалансированность линейных/фазных:** Модель, обученная на файлах с фазными напряжениями, может плохо работать на файлах только с линейными. *Митигация:* Обучать разные модели или использовать Dropout каналов (случайно занулять фазные напряжения в обучении, заставляя сеть учить линейные).