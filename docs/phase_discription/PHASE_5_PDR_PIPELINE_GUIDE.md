# Phase 5 PDR: разметка, анализ и ручной запуск

Это единая инструкция вместо прежних отдельных руководств по датасету и анализу.
Актуальный контракт расширенного литературного бенчмарка — **v6**. Проверенная
v5 сохраняется как исходная версия teacher-разметки и не удаляется до полного
контроля v6.

## Семантика меток v6

- `FORWARD=1` и `REVERSE=0` — два обучающихся класса.
- `UNLABELED=-999` — не третье направление. Такие точки обучают отдельную
  выход применимости (`target_applicable=0`), но не попадают в функцию потерь,
  оценку согласия направления и функцию потерь запаса.
- Нет двух токов для восстановления трёх фаз — `UNLABELED`.
- Токи измерены, но равны нулю/ниже уставки — `REVERSE`.
- Нет восстанавливаемой группы U или все фазные U ниже `0.05 Uном` —
  `UNLABELED`. Одна просевшая фаза не маскирует всю точку.
- Адаптивный MIR может определить направление по памяти U, если текущее U
  пропало. До накопления 10T памятью служит самая ранняя FFT-точка;
  после 10T берётся `t-10T`.
- Для будущего экспертного слоя исходная разметка сохраняется без временного
  сдвига. Точка перехода `expert__VALID` или валидного `expert__FWD` и следующие
  5 мс исключаются только из supervised loss и образуют отдельный transition
  evaluation subset. Сигнал из этих отсчётов не удаляется из входного контекста.

Внутренний порог U для `dataset_peak_phasor` равен
`0.05*sqrt(2)/(3*sqrt(3))`: учтены фазная база, reserve=3 и peak-фазор.
Токовая уставка публичных органов: `0.05*sqrt(2)/20`.

## 1. Smoke-разметка

Открыть `scripts/phase5_experiments/run_pdr_dataset_study.py`, в `run_manual()`:

```python
SMOKE = True
```

Запустить файл. Результат: `data/phase5/pdr_labels_v6_smoke`.
После завершения проверить `status=complete`, наличие всех девяти органов,
отсутствие разогрева адаптивного органа после первого окна Фурье и ненулевое покрытие.

CLI: `python scripts/phase5_experiments/run_pdr_dataset_study.py --smoke`.

### Что нажать для первого запуска v6

1. В VS Code открыть
   `scripts/phase5_experiments/run_pdr_dataset_study.py`.
2. Убедиться, что в `run_manual()` оставлено `SMOKE = True` — сейчас это уже
   безопасное значение по умолчанию.
3. Нажать кнопку **Run Python File** справа вверху либо `F5`.
4. Дождаться завершения двух источников и проверить:
   - `data/phase5/pdr_labels_v6_smoke/open_ee/progress.json`;
   - `data/phase5/pdr_labels_v6_smoke/french_rte/progress.json`.
   В обоих файлах ожидается `"status": "complete"`.
5. Если smoke завершился, сообщить результаты или запустить целевые тесты:

```powershell
& 'C:\ProgramData\anaconda3\python.exe' -m pytest `
  tests/unit/test_pdr_algorithms.py `
  tests/unit/test_pdr_registry.py `
  tests/unit/test_pdr_study.py `
  tests/integration/test_pdr_labeling_pipeline.py -q
```

Если `pytest` жалуется только на создание `%TEMP%/pytest-*`, это проблема прав
локального Windows Temp, а не результат формульного теста. Сам smoke использует
обычные каталоги `data/phase5` и является обязательной интеграционной проверкой.

## 2. Полная разметка

После пробного запуска изменить `SMOKE = False` и запустить тот же файл. По умолчанию:

- Open_EE + French/RTE, все части разбиения;
- все девять органов: пять прежних и четыре новых литературных;
- `SAMPLE_STEP=1` — решение на каждом исходном отсчёте;
- `RECORDS_PER_SHARD=64`;
- lossless-сжатие включено;
- выход: `data/phase5/pdr_labels_v6`.

Повторный запуск с тем же хэшем конфигурации безопасно продолжает готовые фрагменты.
Другой контракт не смешивается с каталогом. Прерывание Ctrl+C теряет только
незакрытый фрагмент.

Перед полным запуском желательно иметь не менее 60–70 ГБ свободного места:
v6 хранит девять органов вместо пяти, а v5 пока остаётся рядом как проверенная
исходная версия. Старую v5 не удалять до завершения v6 и сопоставления общих
пяти органов. После проверки её можно будет отдельно пометить кандидатом на
архивирование/удаление.

Наблюдение из другого PowerShell:

```powershell
while ($true) {
    Clear-Host
    Get-Content data/phase5/pdr_labels_v6/open_ee/progress.json -Raw
    Start-Sleep -Seconds 5
}
```

Не держать `progress.json` открытым блокирующим редактором. Код повторяет
атомарную замену при кратком Windows-lock.

## 3. Основной анализ

После обоих `status=complete` открыть
`scripts/phase5_experiments/analyze_pdr_dataset_study.py`. В `run_manual()` уже заданы:

```python
MODE = "all"
LABEL_DIR = PROJECT_ROOT / "data/phase5/pdr_labels_v6"
OUTPUT_DIR = PROJECT_ROOT / "data/phase5/pdr_analysis_v6"
```

`all` последовательно строит сводку, точное согласие с временной статистикой и
комбинациями девяти органов, аудит сигналов, фактическую маску пригодности 2I+2U,
затем диагностические PNG/CSV. Маска объединяет provenance с наличием конечных
значений, поэтому должна считаться после аудита сигналов. По
умолчанию выбирается 20 примеров на группу
и окно 0,8 с. `FORCED_CASES=(("open_ee", 44574), ("french_rte", 44))`
добавляет два ранее разобранных спорных случая независимо от нового рейтинга.
Выход: `data/phase5/pdr_analysis_v6`.

Расширенный расчёт согласия стал тяжелее прежнего: он дополнительно считает
временные показатели адаптивного органа на уровне записей и до 512 возможных
комбинаций девяти органов. При этом в памяти остаётся только один фрагмент
разметки; прогресс показывается по фрагментам отдельно для каждого источника.
Не запускать этот этап до состояния `complete`
обоих источников и не запускать параллельно второй экземпляр анализа в тот же
`OUTPUT_DIR`.

## 4. Научные проверочные таблицы

После основного анализа запустить
`scripts/phase5_experiments/review_pdr_analysis_results.py`. Его `run_manual()` читает
`pdr_analysis_v6` и добавляет:

- токовые диапазоны в физических `I/Iном`;
- профили источников и частей разбиения;
- чувствительность к дубликатам;
- кластерный бутстреп по уникальным хэшам;
- полный список устойчивых расхождений;
- профили `источник × SPP × f_adc` и чувствительность выводов к способу
  взвешивания осциллограмм;
- пояснение статистических метрик, эмпирические функции распределения,
  тепловые карты и диаграммы
  комбинаций девяти органов.
- паспорт обеих популяций (`all_input` и `pdr_eligible_2i2u`) и data dictionary;
- метод главных компонент и нагрузки, устойчивость KMeans по пяти
  инициализациям, изолирующий лес;
- классификатор различия источников, калибровка и корреляции Спирмена с BH-FDR.

Научный обзор использует только `pdr_eligible_2i2u` для содержательных выводов о РНМ,
но сохраняет все входные записи в паспорте качества. На v5 расчёт многомерного
слоя занимал около минуты; v6 с девятью органами может потребовать больше времени.

Порядок после полной разметки:

1. открыть `analyze_pdr_dataset_study.py`, проверить `MODE = "all"` и нажать
   **Run Python File** / `F5`;
2. после его завершения открыть `review_pdr_analysis_results.py` и так же
   нажать **Run Python File** / `F5`;
3. передать для аудита каталоги `data/phase5/pdr_labels_v6` и
   `data/phase5/pdr_analysis_v6`. Только после этого обновляются численные
   таблицы и рисунки статьи.

## 5. Удаление старых версий

`pdr_labels_v4`, `pdr_analysis_v4` и созданный из них каталог ручного обзора не
удалять до выполнения всех условий:

1. v5 имеет состояние `complete` для Open_EE и French/RTE;
2. число записей/окон совпало с ожидаемым;
3. анализ v5 завершён;
4. выборочные PNG, запись 44574 и French/RTE record 44 проверены;
5. подтверждено, что последующий код открывает v5.

После этого v4 становится кандидатом на удаление. До того момента ничего
в старых каталогах вручную не удалять и не перезаписывать.

## 6. Отбор и экспорт COMTRADE для ручной разметки

Открыть `scripts/phase5_experiments/export_pdr_manual_review_comtrade.py`.
Первый запуск оставить безопасным:

```python
EXPORT_COMTRADE = False
```

Он создаст:

- `data/phase5/pdr_manual_review_v5/candidate_catalog.csv` — все записи и все
  подходящие страты, без ограничения единым списком первых N;
- `selection_preview.csv` — следующий неповторяющийся пакет по квотам.

Квоты `QUOTAS_PER_SOURCE` задаются отдельно для устойчивого расхождения,
фазных/последовательностных органов, пяти диапазонов переходов, околопорогового/большого тока,
аномалий и слепого контроля. Внутри каждой группы выбираются различающиеся по
физическим и временным признакам представители. Один файл экспортируется один
раз, даже если входит в несколько страт.

Для первой проверки программы рекомендуется временно задать одну квоту и имя:

```python
QUOTAS_PER_SOURCE = {key: 0 for key in DEFAULT_QUOTAS}
QUOTAS_PER_SOURCE["persistent_disagreement"] = 1
EXPORT_COMTRADE = True
BATCH_NAME = "pilot"
```

Это даст не более одной Open_EE и одной French/RTE записи. После успешного
открытия вернуть `QUOTAS_PER_SOURCE = dict(DEFAULT_QUOTAS)` и
`BATCH_NAME = None`; пилотные записи автоматически исключатся.

Полный запуск с `EXPORT_COMTRADE=True` создаёт новый каталог
`batch_YYYYMMDD_HHMMSS`, содержащий полную длительность каждого случая, CFG/DAT,
сопроводительный JSON-файл и `batch_manifest.csv`. Аналоговые каналы записываются в A/В через
явные базисы нормировки; пять РНМ и эксперт имеют пары `FWD+VALID`.

Уже экспортированные записи определяются рекурсивно по сопроводительному JSON-файлу и имени CFG.
Поэтому готовые пары можно сортировать внутри `pdr_manual_review_v5` по любым
подпапкам (`accepted`, `ambiguous`, `rejected` и т.п.): следующий запуск их не
выберет. Не удалять сопроводительный JSON-файл и не переименовывать только один файл пары.

Перед массовой ручной разметкой открыть по одному Open_EE и French/RTE файлу в
целевой программе и проверить длительность, масштаб А/В, фазировку и сохранение
дискретных каналов. Обратный импорт будет реализован после фиксации формата, в
котором программа сохраняет изменённые экспертные дискреты.

Готовую экспертную разметку не следует смешивать с исходными `batch_*`.
Рекомендуемая структура статусов, правила переноса комплектов CFG/DAT/JSON и
контракт будущего обратного импортёра приведены в
`docs/phase_discription/PHASE_5_MANUAL_LABELING_GUIDE.md`.

Текущая целевая квота Open_EE выше French/RTE в два раза: Open_EE ближе к
схемам БАВР, а French/RTE используется как внешний домен и источник ёмкостных
режимов. Экспортёр дополнительно просматривает `pdr_manual_labels_v1`, поэтому
`completed`, `ambiguous` и `invalid_quality` не выбираются повторно даже после
удаления исходного автоматического пакета.

Если у отдельной Open_EE-записи отсутствует физический базис `Ip_base`/U-base,
она записывается в `export_failures.csv` и больше не выбирается автоматически.
Остальной пакет продолжается. При прерывании уже готовые sidecar автоматически
собираются в восстановленный манифест на следующем запуске.

### Единый смешанный слепой аудит

Чтобы эксперт заранее не знал, проверяет ли он прежнее решение или размечает
новый случай, профиль `audit` строит один случайный пакет из объединения:

- всех прежних `completed`;
- всех файлов нового стандартного diversity-пакета.

Внутри пакета повторов нет, а доли старых/новых заранее не задаются. Команда:

```powershell
& 'C:\ProgramData\anaconda3\python.exe' `
  'scripts\phase5_experiments\export_pdr_manual_review_comtrade.py' `
  --profile audit --count 100 --export
```

Первый аудит случайно выбрал 74 прежних и 26 на тот момент новых записей, 66
Open_EE и 34 French/RTE. Связь хранится в
`pdr_manual_blind_audit_v1/reference/_service/blind_audit_assignment.csv`; до окончания
разметки этот файл не следует открывать. Рабочие копии находятся в едином
дереве `pdr_manual_blind_audit_v1/labels`. После заполнения статусов выполнить:

```powershell
& 'C:\ProgramData\anaconda3\python.exe' `
  'scripts\phase5_experiments\import_pdr_manual_labels.py' --blind-audit
```

К моменту завершения все 26 новых случаев уже были размечены и импортированы в
основной массив, поэтому первый аудит корректно сформировал 100 независимых
пар. Приоритет согласования определяется не простым несовпадением отсчётов, а
разным числом переключений, сдвигом границ более 20 мс, устойчиво
противоположным направлением и крупным расхождением `VALID`/направления. Малые
сдвиги остаются в таблице, но относятся к `low`. Основной экспертный архив
команда не изменяет: ни первая, ни повторная версия не выбирается автоматически.

Второй независимый пакет находится в
`pdr_manual_blind_audit_v2/reference/batch_20260829_blind_audit_100`: 51 прежний
и 49 новых стандартных файлов, без пересечения с первым аудитом. Подробный
порядок приведён в соседнем `README.md`. Для расчёта его результатов явно
передаются три каталога v2, чтобы не смешивать версии аудита.

## 7. Обратный импорт экспертной разметки

Запустить:

```powershell
& 'C:\ProgramData\anaconda3\python.exe' `
  'scripts\phase5_experiments\import_pdr_manual_labels.py'
```

Сценарий проверяет все статусы, но в обучаемый слой включает только:

- `completed` — обе экспертные головы;
- `invalid_quality` — только отрицательную цель применимости.

`ambiguous` остаётся в аудите и не участвует в loss. Дополнительные каналы
внешней программы допускаются и регистрируются, но не используются. Выход:
`data/phase5/pdr_expert_labels_v1`.

После каждого изменения ручных CFG/DAT этот этап нужно повторить. Он безопасно
пересобирает только компактный производный экспертный слой, не изменяя
`pdr_labels_v5` и исходные COMTRADE.

Основные таблицы:

- `record_audit.csv` — фактическая разметка каждой записи;
- `algorithm_comparison.csv` — сравнение с пятью РНМ: полная доля совпадений
  по отсчётам, среднее, медиана, квартили и стандартное отклонение по
  осциллограммам, а также 95%-е кластерные бутстрэп-ДИ;
- `record_algorithm_comparison.csv` — полный пофайловый рейтинг;
- `expert_quality_flags.csv` — кандидаты слепой перепроверки;
- `expert_group_summary.csv` — сводка по источникам, статусам, split и стратам;
- `records.csv` — сохранённый research-strict split.

## 8. Первый цикл обучения РНМ

Сценарий: `scripts/phase5_experiments/run_phase5_pdr_training.py`.

Для запуска из VS Code/F5 все основные параметры собраны в начале функции
`run_manual()`. Полный weak-запуск по умолчанию имеет следующий объём:

| Параметр | Значение | Смысл |
|---|---:|---|
| `EPOCHS` | 50 | число завершённых проходов обучения в текущем F5-шаблоне |
| `SAMPLES_PER_EPOCH` | 20 000 | число случайно выбранных целевых точек на эпоху |
| `BATCH_SIZE` | 32 | число примеров в одном шаге оптимизатора |
| `MAX_SAMPLES_PER_RECORD` | 64 | максимум индексируемых train-точек одной осциллограммы |
| `VALIDATION_MAX_SAMPLES_PER_RECORD` | 32 | максимум validation-точек одной осциллограммы |
| `EXPERT_VALIDATION_MAX_SAMPLES_PER_RECORD` | 256 | более плотная expert-validation; число файлов всё равно важнее числа точек |
| `WEAK_OPEN_RECORDS` | 4 000 | верхний размер ротационного пула Open_EE одной эпохи |
| `WEAK_FRENCH_RECORDS` | 1 000 | верхний размер ротационного пула French/RTE одной эпохи |
| `ROTATE_WEAK_RECORDS_EACH_EPOCH` | `True` | обходить весь train-split последовательными пулами |
| `MIN_SAMPLES_PER_RECORD_PER_EPOCH` | 4 | гарантированное число используемых точек пригодной записи на weak и expert |
| `TRANSITION_SAMPLING_FRACTION` | 0,5 | целевая доля индексных кандидатов из зон переключений |
| `TRANSITION_SAMPLING_RADIUS_MS` | 20 | полуширина переходной зоны вокруг смены метки |
| `VALIDATION_RECORDS_PER_SOURCE` | 400 | validation-осциллограмм каждого источника |
| `LABEL_STRIDE_SAMPLES` | 5 | шаг отбора доступных целевых точек исходной разметки |

Ротационный sampler гарантированно включает до четырёх различных допустимых
точек каждой структурно пригодной записи текущего пула, после чего заполняет
остаток эпохи по весам источников. Поэтому `SAMPLES_PER_EPOCH` должно быть не
меньше суммарной обязательной квоты; при нарушении сценарий останавливается с
явной диагностикой.

Чтобы редкие переключения не терялись среди длинных стабильных участков,
ограниченный индекс одной записи строится стратифицированно. Границы находятся
по уже сохранённой последовательности teacher-меток; повторно читать и
анализировать сырой сигнал для этого не нужно. По умолчанию до половины из 64
кандидатов берутся из зоны ±20 мс вокруг смены `REVERSE/FORWARD/UNLABELED`, а
остальные — из стабильной части. Если одна страта мала, свободная квота
передаётся другой. Validation эту эвристику не использует и остаётся равномерной.

Сырые сигналы не загружаются целиком в память. На старте читаются sharded-метки
и строится компактный список `(record_id, window_idx)`, а исходная
осциллограмма и спектральные признаки загружаются лениво при запросе batch.
Подготовка индекса теперь отображает прогресс отдельно для каждого источника и
split. Выбранные записи сортируются по `record_id`, чтобы не перечитывать одни
и те же shard-файлы в псевдослучайном порядке.

На Windows рекомендуется первый полный запуск выполнять с `NUM_WORKERS = 0`,
а затем отдельно сравнить скорость с `2` и `4`. Open_EE reader очищает кэш
открытых NPZ-файлов перед передачей dataset дочернему процессу, поэтому
`NUM_WORKERS > 0` поддерживается. При большом числе workers каждый процесс
держит собственный небольшой LRU-кэш и тем самым увеличивает расход RAM и число
одновременных чтений с диска; French/RTE memmap при этом переоткрывается по
пути, а не копируется целиком. Ускорение поэтому не гарантируется.
Workers сохраняются между эпохами (`persistent_workers`), чтобы Windows не
повторял их дорогостоящий запуск на каждой эпохе. Если новый weak-цикл оборвался
до первой эпохи уже после архивирования прежнего результата, повторный запуск
`RESTART_WEAK_FROM` автоматически найдёт выбранный checkpoint в последнем
`archive_*`.

Исключение — ротационный weak-stage: его DataLoader пересоздаётся для нового
пула, поэтому `persistent_workers` там отключён. Если повторный запуск workers
заметно увеличивает паузу между эпохами, сравнить `NUM_WORKERS=0` и `2`.

Сначала проверить weak-stage:

```powershell
python scripts/phase5_experiments/run_phase5_pdr_training.py `
  --stage weak --temporal-mode snapshot_5 `
  --label-stride-samples 5 --smoke
```

Затем запустить полный weak-stage без `--smoke`. По умолчанию используется
SSL-checkpoint `experiments/phase5/pretrain_b_small/best_model.pt`; Open_EE
получает вес 0,8, French/RTE — 0,2.

Из командной строки объём можно задать явно:

```powershell
python scripts/phase5_experiments/run_phase5_pdr_training.py `
  --stage weak --temporal-mode snapshot_5 `
  --label-stride-samples 5 --model-preset small `
  --epochs 40 --samples-per-epoch 20000 --batch-size 32 `
  --weak-open-records 4000 --weak-french-records 1000 `
  --validation-records-per-source 400 `
  --max-samples-per-record 64 `
  --validation-max-samples-per-record 32
```

Флаг `--fixed-weak-record-pool` оставлен только для точного воспроизведения
первого пилота. Для новых научных запусков его использовать не следует.

Во время работы отображаются:

1. полный фактический конфиг и выбранное устройство;
2. прогресс построения индексов по записям;
3. число индексированных train/validation-точек;
4. прогресс каждого train и validation batch;
5. loss, обе группы validation-метрик, скорость и пиковая память GPU после
   каждой эпохи;
6. время подготовки нового train-пула отдельно от чистого времени обучения.

В JSON истории поле `train_pool_prepare_seconds` позволяет проверить цену
ротации и переходной стратификации. Если подготовка начинает занимать заметную
долю полного `total_seconds_with_pool_prepare`, сначала уменьшают размер пула
или `NUM_WORKERS`, а отдельный кэш зон добавляют только после фактического
профилирования.

История сохраняется после каждой эпохи:

- `training_log.jsonl` — одна машинно-читаемая строка на эпоху;
- `training_history.json` — тот же журнал одним JSON-массивом;
- `training_history.png` — loss и метрики направления/применимости;
- `latest_checkpoint.pt` — точка безопасного продолжения;
- `best_model.pt` — лучшая сумма F1 направления и F1 применимости;
- `config.json` — точный контракт запуска.

Название `best_model.pt` означает только максимум заранее выбранной суммы двух
F1 на validation, а не безусловно лучшую инженерную модель. Поэтому для
следующего этапа можно независимо выбрать:

- `SSL_CHECKPOINT_KIND = "best"` или `"latest"` — инициализация weak-stage из
  предыдущего спектрального предобучения;
- `WEAK_CHECKPOINT_KIND = "best"` или `"latest"` — инициализация expert-stage
  из weak-stage.

Для CLI используются `--ssl-checkpoint-kind best|latest` и
`--weak-checkpoint-kind best|latest`. Параметры `--ssl-checkpoint PATH` и
`--weak-checkpoint PATH` имеют более высокий приоритет и позволяют указать
любой конкретный checkpoint. Для научного сравнения желательно прогнать
expert-stage от обоих weak-checkpoint при одинаковом seed и не выбирать вариант
по тому же holdout, на котором затем заявляется итоговое качество.

Для продолжения прерванного запуска установить `RESUME=True` в `run_manual()`
либо добавить `--resume`. При свежем запуске (`RESUME=False`) прежние файлы
автоматически переносятся в подпапку `archive_YYYYMMDD_HHMMSS`, поэтому история
не смешивается и не теряется.

Продолжение полностью поддерживается и для `weak`. Нужно оставить
`STAGE="weak"`, установить `RESUME=True` и задать в `EPOCHS` общее желаемое
число эпох. Например, при checkpoint после 12-й эпохи и `EPOCHS=40` будут
выполнены эпохи 13–40. Восстанавливаются backbone, PDR-голова, оптимизатор и
scheduler. Если процесс остановлен до завершения первой эпохи и
`latest_checkpoint.pt` ещё не создан, продолжать пока не из чего — такой запуск
начинается заново, но уже с отображением прогресса подготовки индекса.

Checkpoint первого пилота был создан до ротации и хранит контракт постоянного
пула. Его нельзя продолжать с `ROTATE_WEAK_RECORDS_EACH_EPOCH=True` обычным
`RESUME`: проверка контракта намеренно остановит запуск. Его `best/latest` можно
использовать как начальные веса нового цикла через `RESTART_WEAK_FROM`, либо
точно воспроизвести старое поведение с отключённой ротацией.

Есть и третий вариант — начать новый weak-цикл с уже обученных весов:

```python
STAGE = "weak"
RESUME = False
RESTART_WEAK_FROM = "latest"  # либо "best"
EPOCHS = 40
```

При этом backbone и PDR-голова загружаются из предыдущего checkpoint, но счётчик
эпох, оптимизатор и scheduler начинают новый цикл. Внутренний индекс первой
эпохи снова равен 0 (в пользовательском прогрессе она показывается как `1/40`).
Если используется тот же выходной каталог, прежний запуск сначала переносится
в `archive_*`, после чего выбранный checkpoint загружается уже из архива.
Эквивалент CLI: `--stage weak --restart-weak-from latest --epochs 40`.

Итого:

| Требуемое действие | Настройки |
|---|---|
| новый weak из SSL-pretrain | `RESUME=False`, `RESTART_WEAK_FROM=None` |
| продолжить оборванный weak | `RESUME=True`, `RESTART_WEAK_FROM=None` |
| новый цикл из прежнего weak | `RESUME=False`, `RESTART_WEAK_FROM="best"` или `"latest"` |

После появления `best_model.pt` выполнить smoke и полный expert-stage:

```powershell
python scripts/phase5_experiments/run_phase5_pdr_training.py `
  --stage expert --temporal-mode snapshot_5 `
  --label-stride-samples 5 `
  --weak-checkpoint experiments/phase5/pdr_weak_snapshot_5_stride5/best_model.pt `
  --smoke
```

По умолчанию экспертный слой берётся из
`data/phase5/pdr_expert_labels_v1`. Если ручная разметка расположена в другом
каталоге с той же структурой (`records.csv` и подпапки `open_ee`/`french_rte`),
путь можно задать параметром:

```powershell
python scripts/phase5_experiments/run_phase5_pdr_training.py `
  --stage expert `
  --weak-checkpoint experiments/phase5/pdr_weak_snapshot_5_stride5/best_model.pt `
  --expert-labels-root D:\путь\к\pdr_expert_labels_v1 `
  --smoke
```

В `run_manual()` тот же параметр называется `EXPERT_LABELS_ROOT`. Там же можно
явно задать checkpoint weak-модели через `WEAK_CHECKPOINT_PATH`, например:

```python
WEAK_CHECKPOINT_PATH = (
    "experiments/phase5/pdr_weak_snapshot_5_stride5/latest_checkpoint.pt"
)
```

Если `WEAK_CHECKPOINT_PATH = None`, путь формируется автоматически из
`TEMPORAL_MODE`, `LABEL_STRIDE_SAMPLES` и `WEAK_CHECKPOINT_KIND`.

Для полного expert запуска убрать `--smoke`. Он использует 75% экспертных
примеров и 25% replay Open_EE; внутри экспертной части Open_EE получает 75%,
French/RTE — 25%. Исходные validation/holdout не переносятся в train.

В полном режиме каждая пригодная экспертная train-осциллограмма получает не
менее `MIN_SAMPLES_PER_RECORD_PER_EPOCH` фактически используемых точек в каждой
эпохе. Ограниченный индекс до 256 точек на экспертный файл переотбирается между
эпохами, сохраняя баланс окрестностей переключений и устойчивых участков.
Replay по умолчанию содержит 1000 Open_EE-файлов одной эпохи и проходит весь
train-split без уже размеченных экспертных train-файлов последовательными
пулами примерно за 39 эпох. Такое исключение предотвращает конфликт ручной и
автоматической цели для одной осциллограммы. Это означает, что
50-эпоховый запуск использует все weak replay-записи хотя бы один раз, а не
повторяет постоянные 300 файлов. Параметры ручного запуска:

- `EXPERT_REPLAY_OPEN_RECORDS = 1000`;
- `ROTATE_EXPERT_SAMPLES_EACH_EPOCH = True`;
- `ROTATE_EXPERT_REPLAY_RECORDS_EACH_EPOCH = True`.

В `training_log.jsonl` поле `train_group_coverage` показывает для каждой
expert/replay-группы число индексированных и реально выбранных записей, долю
охвата и минимум/медиану/максимум точек на файл. Smoke использует 64 примера и
намеренно отключает обязательное покрытие всех экспертных файлов.
Поле `expert_replay_pool` дополнительно хранит номер из 39 пулов и накопленную
долю охваченной replay-популяции; после эпохи 39 она должна стать равной 1,0.

После завершения expert-stage нельзя сопоставлять его training-history с weak
напрямую: validation и эталоны у этапов различаются. Для общей оценки открыть
`scripts/phase5_experiments/evaluate_pdr_expert_holdout.py` и запустить
`run_manual()`. По умолчанию он последовательно проверяет weak/latest,
expert/best и expert/latest только на одинаковой экспертной validation,
сохраняя JSON в `experiments/phase5/pdr_expert_evaluation_v1`. Holdout
добавляется в `SPLITS` однократно после фиксации архитектуры и порогов; для
выбора эпохи или модели он не используется.

Для отдельной итоговой проверки воспроизведения teacher открыть
`evaluate_pdr_full_weak_validation.py`. Он оценивает weak best/latest по всем
1823 Open_EE и 1205 French/RTE validation-записям, но ограничивает число точек
одной осциллограммы. Это длительный запуск после обучения, а не validation
каждой эпохи. Holdout сценарий не читает.

После базовой пары повторить абляции:

1. `snapshot_2`, шаг метки 5;
2. `snapshot_5`, шаг метки 2;
3. `sequence_1_8`, шаг метки 5;
4. шаг 1 — только контрольный дорогой прогон после оценки времени.

Аугментация первой версии не меняет пороги: применяется только совместная
циклическая перестановка фаз и общая смена полярности U/I. Шум и амплитудное
масштабирование пока запрещены, потому что около уставки способны изменить
физически правильную цель.

Текущее окружение `C:\ProgramData\anaconda3\python.exe` пригодно для импорта,
но на проверенной машине не содержит PyTorch/Polars. Обучение следует запускать
из того же окружения проекта, в котором выполнялись предыдущие Phase 5
pretrain-эксперименты. В начале сценарий выводит ошибку до создания checkpoint,
если PyTorch отсутствует.
